name: First Workflow
on: workflow_dispatch #allow for manual start -> this is an event
jobs:
  first-job:   #name of the job
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "hello world!"
      - name: Print Goodbye
        run: echo "Done~Bye~!"
